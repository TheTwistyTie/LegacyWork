public interface Inventory{

	//creates Inventory of defualt size
	public void Inventory();

	//creates Inventory with custom paramaters
	public void Inventory(int rows, int coloums, int hotbarSize);
}